[project]
name = "archiveboxes-workspace"
version = "0.0.0"
description = "Local uv workspace for ArchiveBox packages"
requires-python = ">=3.11"
dependencies = []

[tool.uv]
package = false

[tool.uv.workspace]
members = [
    "abxpkg",
    "abx-plugins",
    "abx-dl",
    "archivebox",
]

[tool.uv.sources]
abxpkg = { workspace = true }
abx-plugins = { workspace = true }
abx-dl = { workspace = true }
archivebox = { workspace = true }
